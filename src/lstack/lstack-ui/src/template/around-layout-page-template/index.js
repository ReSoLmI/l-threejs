import AroundLayoutPageTemplate from './around-layout-page-template'

AroundLayoutPageTemplate.install = Vue => {
  Vue.component(AroundLayoutPageTemplate.name, AroundLayoutPageTemplate)
}

export default AroundLayoutPageTemplate
