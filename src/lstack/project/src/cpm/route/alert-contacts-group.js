import { route } from '../../constructor'
import { AlertContactsPageHub } from './alert-contacts-pagehub'

export const AlertContactsGroups = route('AlertContactsGroups', '告警联系人组', 'groups', {}, AlertContactsPageHub)
