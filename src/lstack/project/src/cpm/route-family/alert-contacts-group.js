import { routeFamily } from '../../constructor'
import { AlertContactsGroups } from '../route'

export const AlertContactsGroupFamily = routeFamily(undefined, AlertContactsGroups)

AlertContactsGroups.setFamily(AlertContactsGroupFamily)
