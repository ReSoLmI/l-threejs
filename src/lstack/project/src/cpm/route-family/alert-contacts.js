import { routeFamily } from '../../constructor'
import { AlertContacts } from '../route'

export const AlertContactsFamily = routeFamily(undefined, AlertContacts)

AlertContacts.setFamily(AlertContactsFamily)
